# UI dev server configuration.
# VITE_API_URL points the dev proxy at your backend; in production the nginx
# container proxies /api to BACKEND_URL instead (see nginx.conf).
VITE_API_URL=http://localhost:__BACKEND_PORT__
VITE_PORT=__FRONTEND_PORT__
